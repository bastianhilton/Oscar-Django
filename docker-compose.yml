version: '3'

services:
  alternate-cms:
    restart: always
    build: ../alternate-cms
    expose:
      - "8000"
    links:
      - postgres:postgres
      - redis:redis
    volumes:
      - alternate-cms-django:/usr/src/app
      - alternate-cms-static:/usr/src/app/static
    env_file: .env
    environment:
      DEBUG: 'true'
    command: /usr/local/bin/gunicorn docker_django.wsgi:application -w 2 -b :8000

  nginx:
    restart: always
    build: ./nginx/
    ports:
      - "80:80"
    volumes:
      - alternate-cms-static:/www/static
    links:
      - alternate-cms:alternate-cms

  postgres:
    restart: always
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data/

  redis:
    restart: always
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  alternate-cms-django:
  alternate-cms-static:
  pgdata:
  redisdata: